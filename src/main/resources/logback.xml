<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="log-path" value="runtime/public/log" />

    <appender name="console-Appender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%-5level %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="Error-File-Appender" class="ch.qos.logback.core.FileAppender">
        <file>${log-path}/prismo-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>%-5level %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="RollingFile-Appender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log-path}/pismo.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${log-path}/pismo.log.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%-5level %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Define a root logger with a ConsoleAppender -->
    <root level="INFO">
        <appender-ref ref="console-Appender" />
        <appender-ref ref="Error-File-Appender" />
        <appender-ref ref="RollingFile-Appender" />
    </root>

</configuration>
